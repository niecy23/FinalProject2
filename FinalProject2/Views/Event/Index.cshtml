@model IEnumerable<Event>

<h2>Events</h2>
<table class="table">
    <tr>
        <th>ID</th>
        <th>Name</th>
        <th>Date</th>
        <th>Time</th>
        <th>Location</th>
        <th>Description</th>
    </tr>

    @foreach (var item in @Model)
    {
        <tr>
            <td>@item.EventID</td>
            <td>@item.EventName</td>
            <td>@item.Date</td>
            <td>@item.Time</td>
            <td>@item.Location</td>
            <td>@item.Description</td>
        </tr>
    }

</table>

